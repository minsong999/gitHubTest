<div><h1>9. blockingLaungae</h1></div>

<div>
    <script>
        alertr("Hello");
        //-> undefined

        alert("Hello")
        console.log("hi")

        setTimeout(() => console.log("lalalala", 5000))
        setTimeout(() => console.log("hi"), 0) //0은 실행하기까지 최소한 기다려아 하는 시간 즉 queue로 이동하고 0ms가 지난 이후 stack에 아무것도 없으면 stack으로 가져와서 실행
        console.log("bye")
    </script>

    <div>
        Web Api에서 setTimeout()은 브라우저에 의해 실행
        -> setTimeout() 안의 코드는 queue에 넣음
        stack은 스택 안에 아무것도 없을 때 Queue에서 가져와서 stack에 넣음

        https://developer.mozilla.org/en-US/docs/Web/API/Window
    </div>
</div>