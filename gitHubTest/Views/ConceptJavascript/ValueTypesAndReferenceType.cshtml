<div><h1>3. ValueTypesAndReferenceType</h1></div>

<div>console.log([10] === [10]);               -> false</div>
<div>console.log({'a':20} === {'a':20});       -> false</div>

<div>let a =50;</div>
<div>let b =a;    --> 두번 째 라인에서 b의 값을 물어보기 때문에 50이 나온다.</div>
<div>a=10;</div>
<div>console.log(b);    -> 50</div>
<div>--------------------------------------------------------------------------------</div>
<div>const sexy = ["현아", "이효리"];</div>
<div>const pretty = sexy;</div>
<div>sexy.push("boom!");</div>
<div>console.log(pretty); -> ["현아","이효리","boom!"]</div>
<div>--------------------------------------------------------------------------------</div>
<div>
    자바 스크립트는 Value Type와 Reference Type으로 나뉜다.
    <div>
        Value Type에는 string, number, boolean, NaN, undefined, null 이 가능하다.
    </div>
    <div>
        Reference Type에는 array, object, function 이 가능하다.
    </div>
</div>
<div>
    sexy의 값이 해당 배열이 아니라 해당 배열을 참조하고 있기 때문에 이와 같은 결과가 나온다.
</div>
<div>
    (즉 Value값을 직접 물어보는 것이 아니라 메모리상에 존재하는 해당 배열의 주소에 해당하는 값을 가져온다.
</div>
